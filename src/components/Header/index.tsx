import { NavBar } from "../Nav";
import { StoryBar } from "../Story";

export const Header = () => {
  return (
    <header className="mb-4">
      {/* <StoryBar /> */}
      <NavBar />
    </header>
  );
};
